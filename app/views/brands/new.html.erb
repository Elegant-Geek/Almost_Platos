<h1>Add a new brand</h1>
  <%= form_with(model: @brand, local: true) do |f| %>
  <%= render "shared/errors", object: @brand %>

  <%= f.label "Brand Name" %>
  <%= f.text_field :name, autofocus: true, required: true, placeholder: "(No duplicates allowed!)" %>
  <%=f.submit "Add Brand"%>

<% end %>
